#include <stdio.h>

#define TXT 1024
#define WORD 30

void toLowerCase(char* word);

void gematria(char* word , char* text);

int Value(char* word);

void clean(char* res);

int arr_size(char* text);

